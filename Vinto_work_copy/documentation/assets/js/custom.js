/**
 *	jQuery scripts for ThemeShifters documentation
 */
jQuery(document).ready(function($) {
	$('#main-nav').onePageNav({
		currentClass: 'current-menu-item',
		scrollSpeed: 800,
		scrollOffset: 40,
		scrollThreshold: 0.5
	});
});